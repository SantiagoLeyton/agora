spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:agora}
    username: ${DB_USER:agora}
    password: ${DB_PASSWORD:agora-local-password}
  jpa:
    show-sql: false

logging:
  level:
    com.agora: DEBUG
    org.flywaydb: INFO

security:
  jwt:
    secret: ${JWT_SECRET:AQIDBAUGBwgJCgsMDQ4PEBESExQVFhcYGRobHB0eHyA=}
